import { StyleSheet } from "react-native";

export default StyleSheet.create({
    image:{
        width: 200,
        height: 200,
        borderRadius: 20,
        resizeMode: "cover",
        padding: 5,
        borderColor: "gray",
        borderWidth: 1,

    }
}); 